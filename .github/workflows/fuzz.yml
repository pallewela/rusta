name: Fuzz

on:
  push:
    branches: [main]
  pull_request:
    paths:
      - 'src/tart.rs'
      - 'fuzz/**'
      - '.github/workflows/fuzz.yml'
  schedule:
    # Daily 04:00 UTC — longer fuzz session against latest main.
    - cron: "0 4 * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: fuzz-${{ github.ref }}
  cancel-in-progress: true

jobs:
  fuzz:
    name: cargo-fuzz (parse_list_json)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6

      - name: Install Rust nightly toolchain (required by cargo-fuzz)
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8  # stable
        with:
          toolchain: nightly

      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32  # v2
        with:
          workspaces: |
            .
            fuzz

      - name: Install cargo-fuzz
        uses: taiki-e/install-action@3771e22aa892e03fd35585fae288baad1755695c  # v2
        with:
          tool: cargo-fuzz

      - name: Build fuzz target (smoke test compilation)
        working-directory: fuzz
        run: cargo +nightly fuzz build parse_list_json

      - name: Run parse_list_json
        working-directory: fuzz
        env:
          # On push/PR, ~30s is enough to catch shallow bugs without
          # holding up the merge queue. On the nightly cron we extend
          # to ~10min for deeper coverage.
          FUZZ_DURATION: ${{ github.event_name == 'schedule' && '600' || '30' }}
        run: |
          set -euo pipefail
          cargo +nightly fuzz run parse_list_json -- \
            -max_total_time="${FUZZ_DURATION}" \
            -print_final_stats=1

      - name: Upload fuzz crash artifacts (if any)
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4
        with:
          name: fuzz-artifacts
          path: |
            fuzz/artifacts/
            fuzz/corpus/
          retention-days: 14
